<div dir="rtl">

# תרגילים – תנאים ב-JavaScript

---

### תרגיל 1 – גיל

</div>

```javascript
const age = 20;
```

<div dir="rtl">

אם גיל 18 ומעלה → הדפס `"מבוגר"`, אחרת → `"קטין"`.

---

### תרגיל 2 – זוגי / אי-זוגי

</div>

```javascript
const number = 7;
```

<div dir="rtl">

בדוק אם המספר זוגי או אי-זוגי והדפס בהתאם.

---

### תרגיל 3 – התחברות

</div>

```javascript
const isLoggedIn = true;
```

<div dir="rtl">

אם אמת → `"ברוך הבא"`, אחרת → `"אנא התחבר"`.

---

### תרגיל 4 – חיובי / שלילי

</div>

```javascript
const num = -5;
```

<div dir="rtl">

בדוק אם המספר חיובי, שלילי, או אפס, והדפס בהתאם.

---

### תרגיל 5 – השוואה

</div>

```javascript
const a = 10;
const b = 20;
```

<div dir="rtl">

השווה בין `a` ו-`b` והדפס מי גדול יותר, או אם שווים.

---

### תרגיל 6 – סיסמה

</div>

```javascript
const password = "1234";
```

<div dir="rtl">

אם הסיסמה נכונה → `"גישה מאושרת"`, אחרת → `"סיסמה שגויה"`.

---

### תרגיל 7 – טווח

</div>

```javascript
const x = 15;
```

<div dir="rtl">

בדוק אם `x` נמצא בין 10 ל-20 כולל, והדפס בהתאם.

---

### תרגיל 8 – ציון תלמיד

</div>

```javascript
const grade = 83;
```

<div dir="rtl">

הדפס את הדירוג לפי הטווחים הבאים:

- 90 ומעלה → `"מצוין"`
- 75–89 → `"טוב"`
- 60–74 → `"עובר"`
- מתחת ל-60 → `"נכשל"`

---

### תרגיל 9 – מזג אוויר

</div>

```javascript
const temp = 28;
```

<div dir="rtl">

הדפס המלצה לפי הטמפרטורה:

- מעל 30 → `"חם מאוד, שתה מים"`
- 20–30 → `"נעים בחוץ"`
- מתחת ל-20 → `"קר, קח ג'קט"`

---

### תרגיל 10 – כניסה מורשית

</div>

```javascript
const age = 20;
const hasID = true;
```

<div dir="rtl">

כניסה מותרת רק אם הגיל 18 ומעלה **וגם** יש תעודת זהות.

---

### תרגיל 11 – הרשאת משתמש

</div>

```javascript
const isAdmin = false;
const isManager = true;
```

<div dir="rtl">

גישה מותרת אם המשתמש הוא admin **או** manager.

---

### תרגיל 12 – שלילה

</div>

```javascript
const hasCard = false;
```

<div dir="rtl">

אם **אין** כרטיס → `"אין גישה"`, אחרת → `"כניסה מאושרת"`.

---

### תרגיל 13 – שם משתמש

</div>

```javascript
const name = "";
```

<div dir="rtl">

אם השם ריק → הדפס `"אורח"`, אחרת → הדפס את השם.

---

### תרגיל 14 – מערך ריק

</div>

```javascript
const arr = [];
```

<div dir="rtl">

בדוק אם המערך ריק והדפס בהתאם.

---

### תרגיל 15 – השוואות

</div>

```javascript
const x = "5";
const y = 5;
```

<div dir="rtl">

בדוק גם `==` וגם `===` והסבר את ההבדל בין התוצאות.

---

### תרגיל 16 – התחברות מערכת

</div>

```javascript
const username = "admin";
const password = "1234";
```

<div dir="rtl">

בדוק האם שם המשתמש **וגם** הסיסמה נכונים, והדפס בהתאם.

---

### תרגיל 17 – הנחות

</div>

```javascript
const age = 67;
```

<div dir="rtl">

הדפס את סוג ההנחה לפי גיל:

- מתחת ל-18 → `"הנחת נוער"`
- 65 ומעלה → `"הנחת גמלאים"`
- אחרת → `"אין הנחה"`

---

### תרגיל 18 – יום בשבוע

</div>

```javascript
const day = "שני";
```

<div dir="rtl">

בדוק אם היום הוא יום עבודה או סוף שבוע, והדפס בהתאם.

---

### תרגיל 19 – מחיר משלוח

</div>

```javascript
const total = 250;
```

<div dir="rtl">

חשב מחיר משלוח לפי סכום הקנייה:

- מעל 300 → `"משלוח חינם"`
- 150–300 → `"משלוח 20 ₪"`
- מתחת ל-150 → `"משלוח 40 ₪"`

---

### תרגיל 20 – מערכת הרשאות

</div>

```javascript
const role = "editor";
```

<div dir="rtl">

הדפס את רמת הגישה לפי התפקיד:

- `"admin"` → `"כל הגישה"`
- `"editor"` → `"עריכה בלבד"`
- `"viewer"` → `"קריאה בלבד"`
- כל ערך אחר → `"אין גישה"`

</div>
